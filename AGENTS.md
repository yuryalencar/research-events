# ReSEARCH Events — Codex Instructions

`CLAUDE.md` is the canonical repository instruction file. Read it completely
before planning, editing, testing, documenting, or committing changes in this
repository.

Apply every rule in `CLAUDE.md` as a Codex instruction. Wherever it names
"Claude", interpret that name as "Codex". This includes, without limitation:

- the spec-driven and Red-Green-Refactor pair-programming phases;
- the requirement for explicit human approval at every phase transition;
- code style, architecture, testing, functional-programming, and learning rules;
- documentation, AI-session summary, README index, release, and Git workflows.

If `CLAUDE.md` and this adapter ever appear to conflict, follow the substantive
rule in `CLAUDE.md`; this file exists only to give Codex the same behavior as
Claude Code without maintaining a second copy that can drift.
