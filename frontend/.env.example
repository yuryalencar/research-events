# Copy this file to .env and fill in your local values.
# .env is gitignored — never commit it.
# Production values are set via Vercel project env vars, not this file.

# Required — base URL of the Go backend (see backend/.env PORT).
# Used by lib/api/client.ts for both public and private requests.
NEXT_PUBLIC_API_URL=http://localhost:8080
